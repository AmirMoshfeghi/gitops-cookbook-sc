apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: sealed-secrets-demo-appset
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: 'https://github.com/your-username/sealed-secrets-demo.git'
      revision: HEAD
      directories:
      - path: 'login-app'
      - path: 'sealed-secrets'
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: 'https://github.com/your-username/sealed-secrets-demo.git'
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: 'sealed'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true